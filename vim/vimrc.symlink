" Settings for vim 

" File type and Syntax Highlighting
filetype on
filetype plugin on
syntax on
   
" autoindent
set autoindent

" Tabs space
set shiftwidth=4
set softtabstop=4

" English spellchecking when needed
if version >= 700
   set spl=en spell
   set nospell
endif

" tab completion
set wildmenu
set wildmode=list:longest,full

" Mouse support in console
set mouse=a

" Line Numbers PWN!
set number

" Highlight search results
set hlsearch